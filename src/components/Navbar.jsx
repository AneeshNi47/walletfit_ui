// Deprecated: replaced by Sidebar + TopBar inside AppShell.
// Kept as a no-op so pages still compile until their own <Navbar /> usages
// are removed in commit 7 (restyle remaining screens).
export default function Navbar() {
  return null;
}
